# État PsychoÉduc Manager — dernière mise à jour : 2026-07-11 (nuit, Comptes multiprofils + Tableau de bord bénéficiaire, Phases 1-2/6)

## Résumé (lisible en 30 secondes) — Comptes à profils multiples + Dashboard bénéficiaire, Phase 1 (fondations/accès) + Phase 2 (Projet de vie) (`CLAUDE-CODE-COMPTES-MULTIPROFILS.md`, `CLAUDE-CODE-DASHBOARD-BENEFICIAIRE.md`, plan détaillé `PLAN_COMPTES_MULTIPROFILS_DASHBOARD_BENEFICIAIRE.md`)
- **Premier vrai portail bénéficiaire du projet** (`/mon-espace`) — jusqu'ici répété comme limite documentée à chaque feature précédente ("aucun portail bénéficiaire n'existe"). Rendu possible sans refonte de schéma : `beneficiaires.profile_id` (nullable, Étape 5) et les policies RLS `profile_id = auth.uid()` sur `beneficiaires`/`evaluations_iga`/`scores_iga` existaient déjà, jamais exploitées par aucun code jusqu'ici.
- **Activation par invitation du praticien, jamais un self-signup public** (mineurs, RGPD) : réutilise `invitations_utilisateurs` (Étape 4, schéma mort depuis sa création, jamais consommé par aucun code) plutôt qu'une nouvelle table. Lien généré et partagé manuellement (aucun service d'e-mail transactionnel dans ce projet), acceptation via `/inscription-beneficiaire`, rattachement `profile_id` via une fonction SECURITY DEFINER (`finaliser_acces_beneficiaire`) car un bénéficiaire ne peut structurellement jamais satisfaire `peut_modifier(organisation_id)`.
- **Comptes multiprofils** : aucune ligne `membres_organisations` créée pour un dossier bénéficiaire — un même profil peut cumuler une organisation (Fondateur/Formateur) ET un ou plusieurs dossiers bénéficiaire, chacun avec son propre historique IGA (`resoudreDestinationConnexion` étendue, sélecteur de bascule dans le layout de chaque côté).
- **"Devenir bénéficiaire" en self-service** (`/devenir-beneficiaire`, accessible depuis le Sidebar Fondateur, `SoloTabs`, et le layout Employeur) : direction manquante initialement — l'invitation couvre le praticien qui fait entrer un tiers, mais pas le cas d'usage principal du document ("un praticien dans son propre système"). Un compte quelconque choisit un autre praticien (liste `profils_publics_formateurs`, déjà publique) et crée sa propre fiche bénéficiaire (`beneficiaires_insert` étendue avec `profile_id = auth.uid()`). Aucun test IGA auto-administré : comme partout ailleurs, une évaluation reste toujours conduite par un praticien.
- **Boussole d'Autonomie** : score IGA + radar SVG natif (aucune librairie ajoutée, même principe que le recadrage de photo par Canvas). Le radar affiche le nombre réel de dimensions du référentiel actif (6 à 8 selon E/A/J/AD), pas un total de 8 forcé arbitrairement malgré la lettre du document.
- **Projet de vie (Phase 2)** : `projets_vie` (Étape 7, jusqu'ici 1:1 et jamais consommée) passée en 1:N (contrainte unique retirée, aucune donnée existante à migrer) — plusieurs projets actifs en parallèle, gérables par le praticien ET en autonomie par le bénéficiaire lui-même (RLS étendue). Fil d'activité calculé à la lecture (jamais stocké) sur 4 des 5 déclencheurs du document (objectif atteint, formation validée, dimension IGA en hausse, palier de réussite) — le 5e ("nouveau frein identifié") explicitement différé, faute de schéma structuré exploitable dans `entretiens.donnees` (jsonb libre) et par prudence RGPD (contenu d'entretien jamais exposé au bénéficiaire dans cette V1).
- **Trois vrais bugs trouvés et corrigés en vérifiant bout en bout** : (1) collisions de sélecteur (`input[name="titre"]`, boutons "Enregistrer"/"Créer") sur 3 tests préexistants (`messagerie-beneficiaires.spec.ts`, `ipp-profil-formateur.spec.ts`, `solo-beneficiaires.spec.ts`) une fois les nouveaux panneaux "Accès bénéficiaire" et "Projets de vie" ajoutés à la même fiche bénéficiaire ; (2) **trou de permission réel et pré-existant** — la matrice posée à l'Étape 7 n'accordait jamais `peut_creer` sur `projets_vie` au rôle `administrateur` (celui qu'un Compte Solo porte sur sa propre organisation), jamais remarqué car aucun code ne consommait cette table jusqu'à cette Phase 2 ; corrigé par alignement sur `objectifs_beneficiaire`/`avis_beneficiaires`, déjà correctement ouvertes.
- **Fixture de test créée avec prudence** : la création de l'organisation fixture pour le compte bénéficiaire e2e a d'abord ajouté par erreur une deuxième organisation "solo" au compte `e2e-solo-fixture` (via le trigger de bootstrap automatique), rendant `getSoloOrganisation()` non déterministe (`.find()` sans `ORDER BY`) — détecté et corrigé en retirant l'adhésion parasite avant que cela ne casse la suite existante.
- **Différé, signalé, non construit en silence** : ICC, Cercles d'apprentissage, Capital social bénéficiaire, ainsi que les points explicitement laissés ouverts par les deux documents source eux-mêmes (impact sur le parcours d'inscription unifié, affichage croisé formateur↔bénéficiaire dans les annuaires publics) — planifiés en Phases 3 à 6, non commencés à cette clôture.
- Vérifié : `supabase/tests/test_comptes_multiprofils_beneficiaire.sql` (token d'invitation, rattachement idempotent, cloisonnement), `supabase/tests/test_projet_de_vie.sql` (self-service RLS, cloisonnement entre bénéficiaires, delete réservé au praticien), `supabase/tests/test_devenir_beneficiaire.sql` (self-service insert, jamais au nom d'un autre profil), `tests/e2e/comptes-multiprofils-beneficiaire.spec.ts`, `tests/e2e/mon-espace-beneficiaire.spec.ts`, `tests/e2e/projet-de-vie.spec.ts`, `tests/e2e/devenir-beneficiaire.spec.ts`, suite complète rejouée sans régression restante, `tsc --noEmit` propre.

# État PsychoÉduc Manager — dernière mise à jour : 2026-07-11 (IPP 4 piliers + profil formateur redesigné)

## Résumé (lisible en 30 secondes) — IPP (4 piliers) + Profil public formateur (`CLAUDE-CODE-IPP.md`, `CLAUDE-CODE-PROFIL-FORMATEUR.md`, tous deux "Validé pour implémentation")
- **Remplacement décidé de l'IPP v1** (`evenements_ipp`/`vue_ipp`, ledger append-only alimenté à la main par le Fondateur, construit lors d'une session précédente) : ne correspond plus au nouveau document, bien plus précis. L'ancien système est **conservé tel quel en base** (aucune perte de données, plus utilisé) ; remplacé par un calcul réel sur **4 piliers** (Impact réel 45%, Qualité/régularité du suivi 25%, Rigueur documentaire 15%, Satisfaction bénéficiaires 15%), en TypeScript standard (`lib/ipp.ts`, logique métier découplée du fournisseur cloud, même principe que `lib/iga.ts`).
- **Aucune nouvelle table de snapshot** : le delta IGA par période de responsabilité est **dérivé** de la première/dernière `evaluations_iga` dans la fenêtre `affectations_personnel.date_debut`→`date_fin` (nouvelle vue `vue_deltas_iga_par_affectation`) — cohérent avec le principe "vue plutôt que table dupliquée". Un bénéficiaire sans évaluation dans la fenêtre est **exclu**, jamais traité comme un delta nul (garde-fou explicite du document).
- **Nouvelle table `avis_beneficiaires`** (témoignages, même vocabulaire de statut que la modération marketplace : en_verification/publie/masque/retiree) — collecte depuis la fiche bénéficiaire (le praticien enregistre l'avis recueilli oralement, aucun portail bénéficiaire n'existe), modération Fondateur minimale sur `/avis` (réutilise `ModerationModal`). **Jamais de nom complet exposé publiquement** (RGPD/mineurs) : affichage "Prénom I." uniquement, calculé à l'affichage, pas en base.
- **Profil public formateur (`/solo/marketplace/formateur/[organisationId]`) et profil privé (`/solo/profil`) redesignés à l'identique** : bloc IPP avec seuil d'affichage explicite (`SEUIL_MINIMUM_ECHANTILLON = 3` bénéficiaires, `[PLACEHOLDER]` à ajuster avec des données réelles) — sous le seuil, "IPP en cours de constitution" plutôt qu'un score neutre trompeur ; paliers de réussite long terme (Réussir 1/2/Terminus, calculés à la volée depuis la durée de suivi, jamais stockés) masqués entièrement s'ils sont tous vides ; liste d'avis publiés.
- **Barèmes de normalisation et seuil d'échantillon explicitement `[PLACEHOLDER]`** — le document source lui-même les qualifie de "à définir ultérieurement avec des données réelles" ; posés à des valeurs raisonnables, signalés en commentaire dans `lib/ipp.ts`, pas cachés.
- **Différé, signalé, non construit en silence** (cf. `PLAN_IPP_PROFIL_FORMATEUR.md`) : tarification/paiement du test IGA, interface dédiée "Rigueur Documentaire" (jauge circulaire, checklist), droit de réponse du formateur aux avis (Phase 2 dans le document source), liste exhaustive des documents obligatoires par type de dossier.
- **Un vrai bug de test trouvé et corrigé en vérifiant bout en bout** : le nouveau formulaire d'avis sur la fiche bénéficiaire (option `<select>` "Parent / Tuteur") entrait en collision avec un `getByText('Parent / Tuteur')` non scopé dans `messagerie-beneficiaires.spec.ts` (modal WhatsApp/Email préexistant sur la même page) — corrigé en scopant l'assertion à l'overlay de la modale, comme déjà fait ailleurs dans la suite.
- Vérifié : `supabase/tests/test_ipp_avis_beneficiaires.sql` (delta IGA correct, cloisonnement RLS d'un avis non modéré, visibilité publique une fois publié), `tests/e2e/ipp-profil-formateur.spec.ts` (cycle complet soumission→modération Fondateur→visibilité publique prénom+initiale, seuil "en cours de constitution", paliers masqués si vides), suite complète rejouée (34/37 puis les 3 échecs restants confirmés en isolation comme de la contention environnementale déjà documentée dans ce projet — rate-limiting Supabase Auth et lenteur ponctuelle du serveur de dev — sauf le bug de sélecteur ci-dessus, réellement corrigé), `tsc --noEmit` propre.

# État PsychoÉduc Manager — dernière mise à jour : 2026-07-10 (nuit, page d'accueil publique)

## Résumé (lisible en 30 secondes) — Page d'accueil publique + refonte du routing racine (`CLAUDE-CODE-Page-Accueil-3.md`)
- **Nouvelle page publique `/`** (marketing, sans authentification) construite à partir de la maquette HTML fournie — structure, textes et animations (calibrage boussole, révélation au scroll) fidèles à l'original.
- **Lien "Accueil" vers `/` ajouté dans les trois menus** (Sidebar Fondateur, onglets Solo, barre Employeur) — sur demande d'Angenor une fois la vitrine construite : sans lui, aucun moyen de revenir à `/` depuis un espace connecté.
- **Collision de route résolue à la racine, cette fois pour de bon** : `/` était jusqu'ici le Cockpit Fondateur (`app/(dashboard)/page.tsx`) protégé par le middleware (`proxy.ts`/`lib/supabase/middleware.ts`, `PUBLIC_PATHS`). Déplacé vers `/dashboard` (`app/(dashboard)/dashboard/page.tsx`) pour libérer `/`. Toutes les autres routes du Cockpit (`/beneficiaires`, `/iga`, etc.) n'ont pas bougé, elles ne collisionnaient jamais.
- **Redirection post-connexion désormais par rôle** (`lib/comptes.ts::resoudreDestinationConnexion`) : Solo → `/solo`, Employeur → `/employeur`, tout le reste (Fondateur, Structure, comptes multi-rôles) → `/dashboard`. Avant cette session, **tous** les comptes atterrissaient sur `/` (l'ancien Cockpit) après connexion, y compris Solo/Employeur — un flou jamais corrigé jusqu'ici, profité de l'occasion pour le clarifier.
- **Middleware réécrit** : `/` passe en public (match exact, pas préfixe — sinon toutes les routes deviendraient publiques), `/inscription` et `/mesurer-iga` ajoutées comme publiques ; un utilisateur déjà connecté visitant `/login` ou `/inscription` est renvoyé vers sa destination de rôle plutôt que vers `/` en dur.
- **Premier flux d'inscription self-service du projet** (`/inscription`) — jusqu'ici, tous les comptes de test/démo étaient créés par SQL direct, aucune UI de création de compte n'existait. Type de compte (Solo/Structure/Employeur) conservé dans `user_metadata` au moment du `signUp()` ; l'organisation n'est créée qu'une fois un contexte authentifié disponible (`finaliserOrganisationEnAttente`, rejouée sans risque à la connexion) — nécessaire car ce projet Supabase exige une confirmation par e-mail avant toute session (vérifié empiriquement, cf. DECISIONS_LOG.md).
- **`/mesurer-iga` : placeholder honnête, pas construit en silence.** L'auto-évaluation IGA publique sans compte est un chantier à part (nouveau modèle de données pour un visiteur anonyme) — hors périmètre de cette session, explicitement signalé plutôt que bâclé.
- **Palette et typographie de la vitrine scopées sous `.landing-page`** (`app/landing.css`), même principe que `.cockpit-fondateur` — n'affecte aucune autre page. Polices Cinzel/Source Sans 3 de la maquette remplacées par les polices déjà chargées globalement (Fraunces/Inter/IBM Plex Mono) pour ne pas dupliquer l'identité typographique du site — écart assumé par rapport à la maquette, documenté dans DECISIONS_LOG.md.
- **13 tests e2e existants mis à jour** (toute assertion `toHaveURL('.../  ')` post-connexion) pour refléter la nouvelle destination par rôle — mécanique mais nécessaire, ce changement touchait le point d'entrée de chaque test qui se connecte.
- Vérifié : `tsc --noEmit` propre, `next build` propre (aucune collision de route), suite Playwright complète rejouée deux fois. Trois vrais bugs de test trouvés et corrigés en cours de route (fixture SQL qui ne pouvait pas se connecter, deux assertions `getByText` non scopées entrant en collision avec le route announcer d'accessibilité de Next.js) ; deux échecs restants confirmés comme de la contention environnementale (rate-limiting Supabase Auth déjà documenté dans ce projet), pas des régressions — détail complet dans DECISIONS_LOG.md.

# État PsychoÉduc Manager — dernière mise à jour : 2026-07-09 (soir, Messagerie interne)

## Résumé (lisible en 30 secondes) — Messagerie interne / Inbox (`PLAN_MESSAGERIE_INTERNE.md`)
- Nouveau système de conversations threadées entre comptes réels (Fondateur ↔ Solo/Structure/Employeur, ou
  staff ↔ staff partageant un bénéficiaire commun) : `/messagerie` (Cockpit Fondateur) et `/solo/messagerie`
  (Compte Solo), point d'entrée "Ouvrir la messagerie interne" depuis la fiche bénéficiaire.
- **Demande de pièce justificative** (Fondateur uniquement) avec suivi de statut en_attente/reçue, pièces
  jointes via bucket Storage privé (`messagerie-pieces-jointes`) et URL signée 5 minutes, jamais d'URL
  permanente stockée. Badge non-lus dans le Topbar (icône déjà présente, jamais câblée jusqu'ici) et `SoloTabs`.
- **Écart documenté avant construction** : "chaque profil (bénéficiaire, parent/tuteur...) dispose d'une boîte
  de réception" n'est réalisable aujourd'hui que pour les comptes ayant une authentification réelle (staff +
  Fondateur) — aucun portail bénéficiaire, aucun compte parent/tuteur n'existe dans ce projet. Non construit
  ici (décision structurante déjà signalée avant ce document), juste documenté à nouveau.
- **`messages` étendue plutôt que recréée** (le document proposait une nouvelle table `messages` en collision
  avec celle de l'Étape 20) : `conversation_id`, `type_document`, `statut_demande` additifs.
- **Cinq bugs RLS/contrainte réels trouvés et corrigés en vérifiant bout en bout** (détail complet dans
  `DECISIONS_LOG.md`) : (1) `INSERT ... RETURNING` sur `conversations` bloqué par la policy SELECT (le créateur
  n'était pas encore visible comme participant de sa propre conversation) ; (2) insertion groupée de 2 lignes
  `conversation_participants` en un seul appel échouant silencieusement (une ligne d'un batch ne voit jamais
  les autres lignes du même batch pour l'évaluation RLS) ; (3) `messages.contenu` NOT NULL violé quand aucune
  note n'accompagne une demande de pièce ; (4) `organisation_fondateur()` non déterministe dès que plusieurs
  comptes portent le rôle fondateur (environnement de test) ; (5) `destinataire_id` jamais renseigné sur une
  demande de pièce, bloquant silencieusement sa propre mise à jour de statut par la policy `messages_update`
  existante (Étape 20).
- Vérifié : `supabase/tests/test_messagerie_interne.sql` (cloisonnement RLS), `tests/e2e/messagerie-interne.spec.ts`
  (cycle complet ouverture→demande→pièce jointe→statut reçu, bout en bout Fondateur/Solo), suite complète
  rejouée sans régression, `tsc --noEmit` propre.

# État PsychoÉduc Manager — dernière mise à jour : 2026-07-09 (Mécanisme IGA → Marketplace)

## Résumé (lisible en 30 secondes) — Mécanisme IGA → Marketplace : spécialités, IPP, recommandations (`PLAN_IGA_MARKETPLACE_MATCHING.md`)
- **Écart réel trouvé** : le document source affirmait l'IPP (Indice de Performance du Praticien) "déjà prévu dans l'architecture" — vérifié faux, aucune trace nulle part. Construit de zéro.
- **IPP en registre append-only** (`evenements_ipp` + vue calculée `vue_ipp`, jamais de score modifiable en place) — même principe que l'argent (Étape 2/15). Écriture réservée au Fondateur (seul rôle qui "vérifie" un témoignage/résultat). Baseline neutre à 50/100 pour tout praticien sans historique.
- **Spécialités par dimension IGA** déclarables sur `/solo/profil` (`profils_publics_formateurs.specialites_dimensions_iga`, colonne additive — le champ texte libre `specialites` existant reste pour l'affichage humain).
- **Moteur de recommandation** sur la page de résultat d'évaluation IGA : conseil générique statique + liste des praticiens spécialisés triés par IPP pour chaque dimension faible, toujours doublé d'un bouton "Contacter le Fondateur" (jamais bloquant si la base de praticiens est vide — point de vigilance explicite du document source).
- **Différé, signalé, non construit en silence** : formulaire de soumission de témoignage/résultat + interface d'approbation Fondateur dédiée — l'ajout d'un événement IPP reste possible dès maintenant par le Fondateur via SQL direct en attendant cette UI. Corrélation automatique avec le module Réussites également différée (delta manuel pour l'instant).
- Vérifié : `supabase/tests/test_ipp.sql` (baseline à 50, cloisonnement en écriture réservée au Fondateur, mise à jour du score après un événement vérifié), `tests/e2e/iga-marketplace-matching.spec.ts` (déclaration de spécialité persistée, repli "Contacter le Fondateur" toujours visible), non-régression complète (marketplace-auto-formations-ui, iga-evaluation — un vrai conflit de sélecteur trouvé et corrigé entre le nouveau panneau "Mise en relation" et un test existant), `tsc --noEmit` propre.

# État PsychoÉduc Manager — dernière mise à jour : 2026-07-08 (IGA multi-référentiel E/A/J/AD)

## Résumé (lisible en 30 secondes) — IGA multi-référentiel : IGA-E, IGA-A, IGA-J, IGA-AD (`PLAN_IGA_MULTI_REFERENTIEL.md`)
- 4 nouveaux référentiels IGA actifs simultanément (Enfance 0-12, Adolescents/Jeunes 13-25, Jeunes adultes
  18-35, Adultes 35+), en plus du référentiel historique à 12 dimensions (inchangé). ~94 nouveaux critères,
  chacun avec son échelle littérale complète 0-4 (aucun raccourci "même logique"), chaque référentiel
  totalisant exactement 100 points (vérifié).
- **Décision structurante** : `referentiels_iga` n'autorisait qu'un seul référentiel actif à la fois (pensé
  pour des versions successives) — contrainte relâchée pour permettre 4 instruments parallèles actifs en
  même temps, routés par tranche d'âge plutôt que remplacés les uns par les autres.
- **Chevauchement 18-25 (IGA-A vs IGA-J)** résolu par choix du praticien (référentiel suggéré selon l'âge,
  toujours modifiable), pas par une règle automatique rigide — cohérent avec le principe "le logiciel
  facilite, ne remplace pas le jugement professionnel" du document de philosophie IGA.
- **IGA-A rédigé par Claude Code** (échelles 0-4 complètes) à partir d'un résumé sommaire fourni — le
  document de référence détaillé n'a jamais été transmis. **Soumis à validation d'Angenor**, contrairement à
  IGA-E/J/AD qui reprennent le texte littéral des fiches fournies telles quelles.
- **Nouveau formulaire de prise d'évaluation** (`/solo/beneficiaires/[id]/evaluations/nouvelle`) — n'existait
  pas avant cette session (les tables `evaluations_iga`/`dimensions_iga` n'étaient lues que pour des
  statistiques, jamais pour une saisie). Score calculé en TypeScript standard (`lib/iga.ts`), jamais fait
  confiance aux poids envoyés par le client (relus systématiquement en base côté serveur).
- **Page de résultat** (`/solo/beneficiaires/[id]/evaluations/[evaluationId]`) : score global, niveau
  (réutilise l'enum existant `evaluations_iga.niveau`, juste un remapping de libellé), dimensions
  fortes/faibles, ajout de pistes d'action (`recommandations_iga`, table déjà existante mais jamais
  utilisée par une UI).
- Vérifié : script SQL confirmant les totaux à 100 points par référentiel, test Playwright de bout en bout
  (évaluation IGA-J complète notée au maximum → 100/100 affiché, dimensions fortes/faibles visibles),
  non-régression de `test_cloisonnement_etape9.sql` (RLS non modifiée, seulement des colonnes/lignes
  additives), `tsc --noEmit` propre.
- **Hors périmètre de cette session** : logique de verrouillage par achat V2 (statuts
  en_développement/disponible_à_achat/actif) — V1 = tous les modules gratuits sur tous les comptes, comme
  demandé explicitement.

# État PsychoÉduc Manager — dernière mise à jour : 2026-07-07 (soir, fiches d'entretien)

## Résumé (lisible en 30 secondes) — Fiches d'entretien Général/Spécialisé (`CLAUDE-CODE-Entretiens-Messagerie.md` §2, terminé)
- Bouton "Créer un entretien" sur la fiche bénéficiaire (`/solo/beneficiaires/[id]`) : choix Général/Spécialisé, chacun avec sa propre page pleine (`/solo/beneficiaires/[id]/entretiens/[entretienId]`) reprenant fidèlement la charte cream/terracotta des mockups fournis (volontairement différente du thème sombre "Boussole d'autonomie" du reste de l'app, sur demande explicite de la spec §0).
- **Fiche générale** : bloc Signalement conditionnel (visible seulement si Type="Signalement"), dimensions IGA à cocher (les 12 dimensions RÉELLES de `dimensions_iga`/Étape 9 remplacent la liste à 8 dimensions du mockup, qui n'était qu'un exemple provisoire — spec §4.1), et blocs "Thématiques de vie" en nombre libre (Constat/Besoin/Action).
- **Fiche spécialisée** : comportements observés en chips (18 presets + ajout libre), verbatim du jeune en italique, analyse/objectifs/stratégies. Classe/scolarisation ajoutées comme colonnes sur `beneficiaires` (propriété du bénéficiaire, pas de l'entretien) et éditables directement depuis la fiche.
- **Réutilisation d'une table existante plutôt que collision de nom** : `entretiens` existait déjà depuis l'Étape 7 (Suivi psycho-éducatif) — un simple journal texte libre (`participants`/`compte_rendu`), jamais consommé par aucune UI, 0 ligne en base. Étendue additivement (`type_entretien`, `statut`, `donnees jsonb`, `valide_par`, `valide_le`) plutôt que dupliquée ; `mene_par` (déjà existant) réutilisé comme praticien. Tout le contenu spécifique aux deux types de fiche va dans `donnees jsonb`, faute de schéma fixe commun (bloc thématiques à nombre libre notamment).
- **Statut Brouillon/Validé sans verrouillage strict** (spec §4.2, décision explicitement laissée ouverte) : une fiche validée devient visuellement lecture seule côté UI, mais aucune contrainte DB n'empêche techniquement de revenir dessus plus tard si Angenor le demande — pas de table d'historique de versions construite pour l'instant (YAGNI tant que le besoin n'est pas confirmé).
- Seule une fiche **validée** s'ajoute à la timeline chronologique unifiée du bénéficiaire (un brouillon n'est pas encore un événement du parcours) ; un brouillon reste visible/reprenable depuis le nouveau panneau "Entretiens" de la fiche bénéficiaire.
- **En-tête co-branding à l'impression (spec §3)** : nouveau composant `EnteteImpression` (visible uniquement à l'impression), appliqué au rapport de bilan + aux deux fiches d'entretien. `organisations.logo_url` ajoutée en base, mais **aucune UI d'upload construite** : aujourd'hui seul `/solo` existe (toujours `type_organisation='solo'`), donc l'en-tête Structure ne s'affichera jamais tant qu'aucun module bénéficiaires propre aux comptes Structure n'existe — mécanisme prêt, mais sans consommateur réel pour l'instant. Signalé pour une session future plutôt que construit en silence en dépassement de la demande.
- **Permission manquante trouvée et corrigée** : le rôle `administrateur` (rôle de tout compte Solo) n'avait jamais reçu accès au module `entretiens` depuis sa création à l'Étape 7 — logique tant qu'aucune UI ne l'utilisait, mais bloquant pour cette feature. Ajoutée en migration additive.
- Vérifié : `supabase/tests/test_entretiens.sql` (cloisonnement RLS inter-organisations, contrainte check type_entretien/statut, confirmation que le rôle `beneficiaire` n'a jamais accès à ses propres entretiens — donnée clinique interne, pas un contenu qui lui est adressé), `tests/e2e/entretiens-beneficiaires.spec.ts` (création brouillon → validation → apparition timeline, pour les deux types), suite de régression bénéficiaires/messagerie/navigation rejouée verte, `tsc --noEmit` propre.
- Messagerie directe (spec §1) déjà entièrement livrée lors d'une session précédente (voir résumé ci-dessous) — non retouchée cette fois.

## Résumé (lisible en 30 secondes) — Messagerie directe WhatsApp/Email bénéficiaires (`PLAN_MESSAGERIE_BENEFICIAIRES.md`, V1 terminée)
- Bouton "Envoyer un message" sur la fiche bénéficiaire (`/solo/beneficiaires/[id]`) : sélecteur à choix unique Bénéficiaire/Parent-Tuteur/Formateur-Responsable, liens `wa.me`/`mailto` générés côté client, aucun envoi ni historique serveur (conforme au mode A "lien simple" de la spec — le mode B API Business reste un chantier à part, explicitement non construit).
- **Aucune nouvelle table** : `beneficiaires`/`parents_tuteurs`/`profiles` avaient déjà `telephone`/`email` depuis l'Étape 1/5 — réutilisés tels quels plutôt qu'une colonne `telephone_whatsapp` dupliquée. "Formateur/Responsable" résolu via `affectations_personnel` (cible polymorphe déjà en place, Étape 4) → `membres_organisations` → `profiles`, sans nouvelle table.
- **Nouvelle colonne additive `organisations.mode_whatsapp_defaut`** (défaut `'lien_simple'`, check contraint à `'lien_simple'|'api_business'`) + nouvelle page privée `/solo/parametres` (bloc Communication) — décision : distincte de `/solo/profil` qui est la vitrine publique, pas un réglage privé.
- Le mode `api_business` est accepté en base (pour ne pas re-migrer plus tard) mais refusé côté serveur (`definirModeWhatsAppDefaut`) tant qu'il n'est pas implémenté — aucun chemin UI ne permet de le sélectionner (radio désactivée, "Bientôt disponible").
- **Limite connue, hors périmètre de cette feature** : aucune UI n'existe encore pour saisir/éditer le téléphone ou l'email d'un bénéficiaire ou d'un parent/tuteur après création (le formulaire "Ajouter un bénéficiaire" ne demande que nom/prénoms) — ces champs existent en base depuis l'Étape 5 mais restent aujourd'hui non éditables depuis l'app. Tant que ça n'est pas construit, le bouton "Envoyer un message" reste désactivé par défaut pour tout nouveau bénéficiaire.
- Vérifié : `supabase/tests/test_messagerie_directe.sql` (résolution des 3 catégories de contact + cloisonnement RLS inter-organisations + contrainte check sur `mode_whatsapp_defaut`), `tests/e2e/messagerie-beneficiaires.spec.ts` (états désactivés du modal sans contact), `tests/e2e/navigation.spec.ts` étendu à `/solo/parametres`, `tsc --noEmit` propre.

## Résumé (lisible en 30 secondes) — Modération Marketplace (Fondateur) + messagerie ciblée sur une offre (`PLAN_MODERATION_MARKETPLACE.md`, terminé)
- Plan exécuté en session interactive avec Angenor, en parallèle de la session concurrente "Publication auto des formations, Étape 3/3" (voir entrée ci-dessous) — aucun fichier partagé touché des deux côtés.
- **Partie A (vue de modération Fondateur)** : `app/(dashboard)/marketplace/page.tsx` étendue (filtre statut, recherche titre/vendeur, colonnes signalements) + 3 actions (`validerOffreMarketplace`/`suspendreOffreMarketplace`/`supprimerOffreMarketplace`, `app/(dashboard)/marketplace/actions.ts`) avec confirmation modale (`ModerationModal.tsx`) et motif optionnel. Valider reste réservé à `is_fondateur()` (contrainte déjà en base depuis l'Étape 16) ; Suspendre/Supprimer suivent `peut_modifier` scoped-organisation.
- **Partie B (messagerie ciblée sur une offre)** : `messages.marketplace_offre_id` (migration `20260717000000`, réutilise entièrement la table `messages` de l'Étape 20, aucune nouvelle policy RLS nécessaire) ; fiche détail d'offre en lecture (`DetailOffrePanel.tsx`) + fil de messages (`MessageThread.tsx`, Fondateur) et réponse vendeur (`MessagesModeration.tsx` + `lib/marketplaceMessages.ts`, partagés `/solo/marketplace` et `/employeur`).
- **`log_audit_action()` RPC** (migration `20260716000000`) : seule fonction autorisée à écrire dans `audit_logs` depuis une Server Action (append-only strict, aucune policy d'écriture directe pour `authenticated` depuis l'Étape 1).
- **Un vrai bug de test trouvé et corrigé (pas un bug applicatif)** : `tests/e2e/marketplace-messagerie.spec.ts` vérifiait l'envoi du message avec un `getByText` page-entière juste après le clic — qui matchait en réalité le `<textarea>` encore rempli (jamais vidé sauf succès), masquant un vrai échec d'envoi. Corrigé en attendant le retour du bouton à l'état "Envoyer" puis en scopant la vérification à la liste réelle des messages (`ul li p`). Un second symptôme (bouton "Envoyer" resté désactivé après `fill()` sur cette page dense) corrigé par un pattern retry-fill (`expect(async () => {...}).toPass()`) plutôt qu'un fill unique fragile.
- Compte de test dédié `e2e-fondateur-fixture@psychoeduc-manager.local` (rôle `fondateur` réel) créé pour ces tests — l'ancien compte fixture n'avait que le rôle `administrateur`, jamais vérifié sur une action réservée au Fondateur avant.
- Vérifié : `supabase/tests/test_moderation_marketplace.sql` (cloisonnement RLS + `log_audit_action`) vert ; `tests/e2e/marketplace-moderation.spec.ts` + `tests/e2e/marketplace-messagerie.spec.ts` verts sur 2 runs consécutifs après correctif ; `tsc --noEmit` propre.

## Résumé (lisible en 30 secondes) — "Publication automatique des formations + badge de vérification" (3 étapes validées une à une)
- Fonctionnalité demandée et construite en 3 étapes séparées, avec validation d'Angenor entre chaque étape ("continue" à chaque fois) : **1) schéma, 2) logique de création automatique, 3) UI**. Détail complet dans `DECISIONS_LOG.md`.
- **Étape 1 (schéma)** : nouveau statut `marketplace_offres.statut = 'visible_en_verification'` (visible publiquement immédiatement, avant validation Fondateur), `formation_id` de référence croisée, RLS et masquage par signalement étendus, `profils_publics_formateurs` enrichi (`photo_url`, `cv_url`, `cv_texte`).
- **Étape 2 (logique)** : `lib/marketplaceAutoPublish.ts` — en TypeScript standard (pas de trigger SQL), une formation créée par un compte Solo/Structure obtient automatiquement une offre marketplace liée, dès l'insertion (indépendamment du statut `brouillon` interne). Un risque de duplication entre les deux vues marketplace détecté et corrigé avant qu'il ne devienne visible.
- **Étape 3 (UI)** : badge "NOUVEAU · EN VÉRIFICATION" (ambré, cohérent avec la palette existante) sur les cartes et la page détail ; nouvelle page détail d'offre formation (`/solo/marketplace/formation/[id]`, avec programme/cours, avis, encadré formateur) ; nouveau profil public formateur en lecture (`/solo/marketplace/formateur/[organisationId]`, stats calculées, badge Vérifié dérivé, bouton Contacter via la messagerie interne) ; `/solo/profil` enrichi (upload + recadrage carré de photo côté navigateur, CV structuré, badge Vérifié).
- **Découverte importante** : une session Claude Code concurrente travaillait en parallèle sur la file de vérification Fondateur (`PLAN_MODERATION_MARKETPLACE.md`, `app/(dashboard)/marketplace/*`) — non dupliquée, fichiers non touchés pour éviter toute collision.
- **Un troisième trou de permission (le plus large) trouvé et corrigé** : `marketplace_offres_delete` n'avait aucune clause `organisation_id` du tout — aucun compte Solo/Employeur ne pouvait jamais supprimer sa propre offre marketplace.
- Toutes les nouvelles fonctionnalités vérifiées individuellement (SQL + Playwright, avec patience face à une contention réseau/serveur partagé ponctuelle avec la session concurrente — voir note ci-dessous), mais **le dernier run de la suite Playwright complète a été instable** à cause de cette contention partagée (confirmé par un timeout de connexion Postgres direct), pas à cause d'une régression de code.

## Résumé (lisible en 30 secondes) — session précédente (mode autonome : marketplace généraliste + gestion bénéficiaires + module Employeur)
- **Session en mode autonome complet** (`PROMPT-MODE-AUTONOME.md`) : détail complet, hypothèses et points en attente d'avis dans **`JOURNAL-AUTONOMIE.md`** (à lire en priorité).
- **Marketplace généralisé** : découverte qu'une table générique (`marketplace_offres`, produit/service/formation, modération Fondateur) existait déjà depuis l'Étape 16 mais n'était utilisée par aucune UI — étendue (médias, stock/livraison) plutôt que fusionnée avec `formations` (décision documentée, en attente d'avis). Pages `/solo/marketplace` (navigation publique unifiée, filtres, favoris, achat, Modifier/Supprimer) + `/solo/favoris` + **nouveau module `/employeur`** (même CRUD d'offres que le Compte Solo).
- **Trois trous de permission réels trouvés et corrigés** : `administrateur` avait `peut_supprimer=false` sur `formations` ET `beneficiaires` ; `marketplace_offres_delete` n'avait carrément aucune clause `organisation_id` (aucun compte Solo/Employeur ne pouvait jamais supprimer une offre, quel que soit son rôle).
- **Un bug de routing réel corrigé** : `app/page.tsx` (prototype oublié depuis les tout premiers commits) entrait en collision silencieuse avec `app/(dashboard)/page.tsx` sur la route `/` — Next.js ne le signalait pas comme erreur de build (contrairement à la collision `(solo)`/`(dashboard)` déjà rencontrée), il choisissait l'un des deux de façon non déterministe. Supprimé.
- **Gestion des bénéficiaires complète** : suppression avec garde-fou, classification automatique, file d'attente de demandes, messagerie typée (signalement jamais visible du bénéficiaire, vérifié par test), fil chronologique unifié.
- **Profil vendeur enrichi** : note de satisfaction réelle combinant avis de formations ET d'offres marketplace, compteur de clients marketplace réel, badge "NOUVEAU VENDEUR" réel (jamais de chiffre inventé).
- **Revue de sécurité complète effectuée** : un problème réel trouvé et corrigé (recherche marketplace vulnérable à une injection de filtre PostgREST) — détail dans `JOURNAL-AUTONOMIE.md`.
- 18/18 tests Playwright verts, 4/4 suites SQL vertes, `tsc --noEmit` propre.
- **Points explicitement en attente de l'avis d'Angenor** (ne pas construire dessus sans confirmation) : coexistence des deux systèmes marketplace, reste de `PROMPT-MARKETPLACE-ENGAGEMENT-1.md` (recommandations, parcours, fidélité, paiement).

## Résumé (lisible en 30 secondes) — session précédente (Phase C, édition/suppression formations)
- Les 25 étapes de l'architecture v5 sont closes (schéma : 124 tables, 14 vues, 400+ policies).
- **Phase C (cette session)** : implémentation fidèle de `PROMPT-EDIT-DELETE-FORMATION.md` (Modifier/Supprimer une formation, avec confirmation modale et blocage si des inscriptions existent) et de `PROMPT-AMELIORATIONS-COMPTE-SOLO-1.md` en quasi-totalité — objectifs/jalons par bénéficiaire, rapport de bilan imprimable, avis/notes des élèves, profil public enrichi, centre de notifications, calendrier de séances avec rappel, relance des élèves inactifs, ressources téléchargeables.
- **Un vrai bug de sécurité/fonctionnalité corrigé au passage** : le rôle `administrateur` (celui de tout propriétaire de Compte Solo non-fondateur) avait `peut_supprimer=false` sur `formations` depuis l'Étape 8 — sans le correctif, aucun compte Solo ordinaire n'aurait jamais pu réellement supprimer sa propre formation. Corrigé par une policy RLS additionnelle scoped aux organisations `type_organisation='solo'` uniquement (la gouvernance structure/employeur reste inchangée).
- 12/12 tests Playwright verts, 2/2 suites SQL vertes, aucune régression. Playwright est désormais configuré `workers: 1` (Supabase Auth rate-limite les connexions concurrentes — les échecs en parallèle n'étaient pas un bug applicatif).
- Reste, sur demande d'Angenor : sauvegardes automatiques + orchestration financière applicative + intégration d'un prestataire de paiement réel.

## Ce qui a été construit cette session (Phase C — édition/suppression + améliorations Compte Solo)
- **Édition/suppression de formation** (`app/solo/formations/`) : le formulaire "Ajouter" devient "Modifier « titre »" via `?edit=<id>`, `modifierFormation` et `supprimerFormation` (Server Actions). Suppression bloquée côté serveur (pas seulement dans l'UI) si des `inscriptions_formations` existent — message explicite invitant à archiver à la place. Modale de confirmation thème sombre (`app/solo/_components/ConfirmModal.tsx`) plutôt qu'un `window.confirm()` natif.
- **Migration `20260707000000_compte_solo_ameliorations.sql`** : tables `objectifs_beneficiaire`, `avis_formations` (+vue `vue_notes_formations`), `profils_publics_formateurs`, `seances`, `ressources_formation` + bucket Storage privé `ressources-formations` (policies `storage.objects` scopées par `formation_id` en préfixe de chemin) ; fonction générique `notifier_membres_organisation()` + triggers sur nouvelle inscription / paiement confirmé / nouvel avis (réutilise la table `notifications` de l'Étape 20, aucune nouvelle table de notifications) ; fonction `verifier_rappels_seances()` (rappel calculé paresseusement à l'ouverture du Calendrier, pas de cron dans ce projet) ; correctif de permission `administrateur` sur `inscriptions_formations/progression_formation/certificats/paiements_formation`. **Migration `20260707010000_formations_delete_solo.sql`** : policy RLS additionnelle permettant la suppression de formations aux comptes Solo (voir bug ci-dessus).
- **Objectifs & jalons + messagerie + rapport de bilan** : `app/solo/beneficiaires/[id]/page.tsx` (fiche complète : cadran IGA, historique, jalons avec avancement à_venir→en_cours→atteint, messagerie directe réutilisant la table `messages` existante) et `app/solo/beneficiaires/[id]/rapport/page.tsx` (page imprimable, `window.print()` — aucune librairie PDF ajoutée, le navigateur sait déjà exporter en PDF ; overrides CSS `@media print` dans `globals.css`). `app/solo/beneficiaires/actions.ts` : ajout d'un formulaire "Ajouter un bénéficiaire" qui manquait purement et simplement (aucun compte Solo non-fondateur ne pouvait en créer un auparavant).
- **Avis/notes + profil public enrichi** : panneau "Mes formations suivies" sur la Vue générale (acheteur note 1-5 + commentaire via `NoterFormation.tsx`, upsert sur `avis_formations`) ; note moyenne affichée sur chaque fiche formation (formateur) et sur le Profil public (agrégée) ; `app/solo/profil/page.tsx` permet d'éditer bio + spécialités (`profils_publics_formateurs`) et affiche des indicateurs de progression du formateur (bénéficiaires accompagnés, ancienneté).
- **Centre de notifications** : `NotificationsBell.tsx` (cloche avec badge non-lus, dropdown, "tout marquer comme lu") dans le layout Solo — un seul point d'entrée pour inscription/paiement/avis/rappel, alimenté par les triggers de la migration.
- **Calendrier de séances** : `app/solo/calendrier/` — planifier un rendez-vous de suivi (bénéficiaire) ou une session live (formation), historique, rappel automatique en notification interne (pas d'email, cf. limite documentée ci-dessus).
- **Relance des élèves inactifs** : dans `formations/page.tsx`, détection des inscriptions `en_cours` sans progression depuis 14 jours ; bouton "Relancer" ouvrant une modale avec un message suggéré, **jamais envoyé sans validation explicite du formateur** (`RelancerEleve.tsx` + `relance-actions.ts`).
- **Ressources téléchargeables** : upload/suppression par formation (`ressources-actions.ts`), téléchargement via URL signée générée côté serveur (bucket privé, jamais de lien public statique).
- **Nouveaux tests permanents** : `tests/e2e/solo-beneficiaires.spec.ts` (bénéficiaire + jalon + message + rapport), `tests/e2e/solo-ameliorations.spec.ts` (calendrier, ressources, profil, cloche notifications) ; `solo-formations.spec.ts` étendu (édition/suppression) et désormais nettoie ses propres données de test via la suppression réelle. `navigation.spec.ts` étend aux routes `/solo/calendrier`.
- **`playwright.config.ts`** : `workers: 1` — la suite complète à 6 workers échouait par intermittence sur le login (rate-limit Supabase Auth), pas un bug applicatif ; confirmé en rejouant à 1 worker (12/12 verts) après un run à 6 workers (5 échecs, tous au login).

## Ce qui avait été construit avant (Phase A + Phase B, Compte Solo initial)
- **Phase A — 7 pages manquantes** : `app/(dashboard)/iga`, `capital-social`, `agr`, `insertion`, `marketplace`, `evenements`, `intelligence-economique` — toutes des lectures réelles sur les tables/vues déjà existantes depuis les 25 étapes (aucune nouvelle table). Sidebar (`NAV_SECTIONS`) et `modules/page.tsx` (grille `MODULES_DETAIL`) mis à jour en conséquence. 21 entrées de sidebar au total désormais.
- **Phase B — schéma** : migration `20260706000000_compte_solo_formations.sql` — extension de `formations` (`mode_transmission`, `prix`, `devise`, statut `suspendue` ajouté), policy publique `formations_select_publique` (formations `publiee` visibles de tous), vue `vue_marketplace_formations`, tables `inscriptions_formations`/`progression_formation`/`certificats`/`paiements_formation` (append-only), trigger `check_completion_formation` (auto-complétion + certificat à 100% des cours), RLS complet vendeur/acheteur. Migration additive `20260706001500_formations_duree_texte.sql` (`duree_texte` format libre). Testé par `supabase/tests/test_compte_solo_formations.sql` (5/5 vérifications : vue marketplace, non-déclenchement à progression partielle, déclenchement à 100%, cloisonnement tiers/acheteur).
- **Phase B — frontend** : `app/solo/` (segment réel, pas un route group — voir décision ci-dessous) : `layout.tsx` (sélecteur de contexte + badge FONDATEUR + lien retour Cockpit), `page.tsx` (tableau de bord avec `IgaDial` réutilisé, aperçu bénéficiaires, aperçu marketplace public), `beneficiaires/page.tsx`, `formations/page.tsx` + `actions.ts` (CRUD complet du cycle de vie), `revenus/page.tsx` (avec avertissement honnête sur l'absence de prestataire de paiement), `profil/page.tsx`, `marketplace-actions.ts` (`sInscrireFormation`). Lien "Mon espace Solo" ajouté à la Topbar du Cockpit Fondateur.
- **Comptes de test** : `e2e-solo-fixture@psychoeduc-manager.local` (type `solo`, org "E2E Solo Fixture" dédiée) créé en appliquant proactivement la leçon `auth.identities` + tokens `''` — fonctionnel du premier coup.
- **Nouveau test** `tests/e2e/solo-formations.spec.ts` : connexion avec le compte solo dédié → création d'une formation → Publier → Suspendre → Archiver, vérifie l'absence de boutons d'action résiduels et l'absence d'erreurs JS. `tests/e2e/navigation.spec.ts` étendu aux routes `/solo/*`.

## Précédent : refonte thème sombre "Boussole d'autonomie" (session du 2026-07-05, plus tôt)

## Ce qui a été construit cette session (refonte thème sombre)
- **`app/globals.css`** : tokens `bg-base/bg-surface/bg-card/border-soft/accent-gold(-dim)/accent-teal(-dim)/text-primary/text-muted/danger`, halo teal ambiant, scrollbar fine thématisée.
- **`app/layout.tsx`** : ajout d'IBM Plex Mono (`next/font/google`) en plus de Fraunces/Inter, exposé comme `font-data`.
- **`_components/ui.tsx`** : `PageHeader` (icône lucide + texte eyebrow), `Panel`, `StatCard` (icône lucide obligatoire, valeur en `font-data`), `StatusPill` (pastille animée pour les statuts "ok"), `DataTable`, `EmptyState`, et **nouveau composant `IgaDial`** (cadran SVG signature pour le score IGA moyen).
- **`_components/Sidebar.tsx`** : rétractable (76px ↔ 260px), fermé par défaut, bouton bascule circulaire sur la bordure, tooltips CSS en mode réduit, scroll indépendant (`overflow-y-auto` + scrollbar fine), état persistant à travers la navigation (propriété naturelle des layouts Next.js App Router, vérifiée par test).
- **`_components/Topbar.tsx`** : icônes lucide (`Search`, `Bell`, `MessageSquare`), thème sombre.
- **14 pages du menu** : tous les emoji remplacés par des icônes `lucide-react` cohérentes (mêmes icônes que leur entrée de sidebar respective), aucune donnée ni logique de requête modifiée.
- **`app/login/page.tsx`** : restylé en thème sombre (n'était pas dans le mockup fourni, mais la consigne "aucun fond blanc résiduel" imposait de le traiter).
- **Deux bugs d'hydratation React réels détectés et corrigés** en testant avec un vrai navigateur (pas seulement des requêtes HTTP) : `type="search"` → `type="text"` (Topbar, session précédente) et `suppressHydrationWarning` ciblé sur les inputs email/mot de passe de la connexion (`caret-color` injecté par Chrome avant hydratation).
- **`tests/e2e/navigation.spec.ts`** (créé la session précédente, toujours vert) : couvre les 14 pages.
- **`tests/e2e/dashboard.spec.ts`** et **`tests/e2e/auth.spec.ts`** : ajustés pour le nouveau comportement (sidebar fermé par défaut → l'email n'est visible qu'après ouverture ; sélecteurs de texte scopés à `main` pour éviter l'ambiguïté avec les tooltips cachés dans le DOM).

## État global
134 tables, 19 vues (+`vue_satisfaction_vendeur`), 400+ policies (RLS incluse dès la création, + 1 bucket Storage privé). Frontend : écran de connexion + 21 pages du Cockpit Fondateur + 9 pages du Compte Solo (`/solo/*`, dont 2 dynamiques `[id]`) + 1 page Employeur (`/employeur`), thème sombre complet. 18/18 tests Playwright verts, 4/4 suites SQL vertes.

## Reste à faire (prochaine session, sur demande d'Angenor)
- **Lire `JOURNAL-AUTONOMIE.md`** : contient les points explicitement en attente de l'avis d'Angenor (coexistence des deux systèmes marketplace, reste de `PROMPT-MARKETPLACE-ENGAGEMENT-1.md`).
- `PROMPT-MARKETPLACE-ENGAGEMENT-1.md` reste : "Vu récemment", recommandations personnalisées par profil IGA, section de rotation des nouveaux vendeurs (le badge existe déjà), parcours recommandés, programme de fidélité, tout ce qui dépend d'un prestataire de paiement.
- Sauvegardes automatiques (schéma `sauvegardes_export` prêt, Étape 24).
- Orchestration applicative TypeScript (paiements/commandes → `mouvements_financiers`).
- Intégration d'un vrai prestataire de paiement (aujourd'hui : `paiements_formation` ET `marketplace_commandes` sont de simples registres, aucun mouvement d'argent réel).
- Écran "changer mon mot de passe" (mot de passe temporaire du compte fondateur toujours à changer).
- Barre de recherche du Cockpit Fondateur : champ visuel seul, pas encore de logique de recherche branchée (distincte de la recherche marketplace, qui elle est fonctionnelle).
- Mini-cadran IGA par bénéficiaire (mentionné dans `DESIGN-SPEC-1.md` comme motif récurrent, pas encore construit — actuellement seul le score moyen agrégé en a un).
- Messagerie bénéficiaire : à ce jour un seul sens (formateur → bénéficiaire) pour le suivi/entretien, le bénéficiaire n'a pas de portail pour répondre — aucun portail bénéficiaire n'existe dans l'app à ce jour, en construire un serait une décision structurante à valider avec Angenor.
- Calendrier : vue liste uniquement (pas de grille mensuelle visuelle) — suffisant pour la V1 du Compte Solo, une vraie vue calendrier pourrait être demandée plus tard.
- Rappels de séances et relance des inactifs : notification interne uniquement, aucun email — pas d'infrastructure d'envoi d'email dans le projet à ce jour.

## Dette technique / points de vigilance
- Toute future création de compte par SQL direct doit inclure une ligne `auth.identities` + colonnes token en `''` jamais `NULL` (appliqué avec succès une 3ᵉ fois pour le compte fixture Solo).
- Mot de passe temporaire du compte fondateur à changer.
- Réconciliation `transactions_wallet` ↔ `mouvements_financiers` toujours en attente.
- Si un jour un mini-cadran IGA par bénéficiaire est demandé, réutiliser le composant `IgaDial` existant (accepte déjà une prop `value` générique) plutôt qu'en recréer un.
- **Piège Next.js 16 à ne pas reproduire** : un dossier entre parenthèses (`app/(nom)/`) est un *route group* — il n'ajoute jamais de segment à l'URL. Pour une vraie route `/solo`, il faut un dossier `app/solo/` réel (leçon apprise après une collision de build "two parallel pages that resolve to the same path" en ayant initialement construit `app/(solo)/`).
- **Toute Server Action bindée directement sur `<form action={...}>` doit retourner `Promise<void>`**, pas `{error}` — sinon `next build`/`tsc` échoue (React 19/Next 16 exigent ce type précis). Si l'appel se fait de façon impérative depuis un composant client (pas via `<form>`), la fonction PEUT retourner `{error}`, mais il faut alors appeler `router.refresh()` soi-même après succès : `revalidatePath()` seul n'actualise pas une page déjà montée quand l'action n'est pas déclenchée par une soumission de formulaire. Bug réel rencontré et corrigé sur `ConfirmModal.tsx` (la suppression de formation semblait ne rien faire tant que ce `router.refresh()` manquait).
- **Fichiers Storage orphelins** : la suppression d'une formation cascade sur `ressources_formation` (FK) mais PAS sur les objets réels du bucket Storage (système séparé, pas de cascade SQL possible) — les fichiers restent dans `ressources-formations` même après suppression de la ligne. Non bloquant à ce stade (bucket de test), mais à traiter si le volume devient réel (job de nettoyage périodique ou suppression explicite du fichier avant suppression de la formation).
- **Playwright et RLS `hasText`** : un `div` ancêtre "contient" toujours le texte de ses enfants — `.filter({hasText}).first()` ou `.last()` peuvent tous deux sélectionner le mauvais niveau selon la profondeur de l'arbre. Solution appliquée systématiquement : scoper sur une classe CSS propre à la carte/ligne ciblée (`div.py-4`, `div.py-3`) plutôt que sur `div` générique.
- **Suite Playwright et rate-limit Supabase Auth** : `playwright.config.ts` force `workers: 1`. Ne pas paralléliser sans vérifier que le rate-limit GoTrue le supporte (comportement observé : échecs de login en cascade dès 6 workers, aucun rapport avec le code applicatif).
- **Deux systèmes de marketplace coexistent** : `formations` (pédagogique, auto-publication sans modération) et `marketplace_offres` (produit/service, modération Fondateur obligatoire depuis l'Étape 16). Unifiés uniquement en lecture via `vue_marketplace_publique`. Voir `JOURNAL-AUTONOMIE.md` pour la décision complète et pourquoi une fusion n'a pas été tentée sans confirmation d'Angenor.
- **Ne jamais interpoler une entrée utilisateur libre directement dans une chaîne de filtre PostgREST** (`.or(\`...${q}...\`)`) sans la nettoyer d'abord — trouvé et corrigé dans `app/solo/marketplace/page.tsx` (caractères `,().` pouvaient détourner le filtre prévu). RLS reste la protection de fond, mais ce n'est pas une raison de laisser ce genre de trou ouvert.
- **Nouveau motif de bug de permission à surveiller pour tout futur module Compte Solo/Employeur** : le rôle `administrateur` a systématiquement `peut_supprimer=false` par défaut dans les seeds de permissions de l'architecture v5 (voulu pour les structures/employeurs à plusieurs intervenants) — et pour `marketplace_offres`, la policy de delete d'origine (Étape 16) n'avait même aucune clause `organisation_id` du tout. Déjà trouvé et corrigé trois fois (`formations`, `beneficiaires`, `marketplace_offres`) via une policy RLS additionnelle scopée par type d'organisation. Si un futur module ajoute une action de suppression, vérifier ce point AVANT de découvrir le bug en test.
- **`app/page.tsx` (route racine) doit rester supprimé** : c'était un prototype thème clair oublié depuis les tout premiers commits, en collision silencieuse avec `app/(dashboard)/page.tsx` sur `/` — contrairement à la collision `(solo)`/`(dashboard)` déjà rencontrée, Next.js/Turbopack n'a jamais signalé d'erreur de build pour celle-ci, il choisissait l'un des deux fichiers de façon non déterministe selon l'état du serveur de dev. Si un jour un vrai besoin de page à la racine hors du groupe `(dashboard)` apparaît, bien vérifier qu'aucun autre fichier ne résout déjà vers le même chemin.
- **Un dev server Next.js qui tourne depuis des heures peut masquer un vrai bug plutôt qu'un cache périmé** : avant de conclure "c'est juste le cache Turbopack, je redémarre", vérifier s'il n'existe pas un second fichier de route en conflit — le redémarrage seul n'aurait pas suffi ici (le bug persistait après redémarrage complet, la vraie cause était `app/page.tsx`).
- **Deux sessions Claude Code peuvent partager le même serveur de dev (port 3000) et le même projet Supabase distant sans le signaler explicitement.** Détecté cette session via des routes admin (`/marketplace`, `/communication`) apparaissant dans les logs du serveur alors que je ne les visitais pas, puis confirmé par un vrai timeout de connexion Postgres direct (`supabase db query`) — la lenteur ambiante (logins/pages à 10-25s, tests Playwright échouant sur de simples timeouts par défaut de 5s) n'était donc PAS une régression de code mais une contention de ressources partagées. Le redémarrage du serveur ne règle rien dans ce cas (contrairement au cas `app/page.tsx` ci-dessus) puisque le serveur redémarré est de toute façon repartagé aussitôt. Réflexe à appliquer avant de chercher un bug de perf : vérifier les logs serveur pour des requêtes vers des routes qu'on n'a pas soi-même visitées.
- **`app/(dashboard)/marketplace/*`, `app/(dashboard)/_components/*`, `app/(dashboard)/page.tsx`, `app/(dashboard)/layout.tsx` et `app/globals.css` ont été modifiés par une session concurrente pendant cette session** (chantier de modération marketplace, `PLAN_MODERATION_MARKETPLACE.md`) — non touchés ni commités par moi pour éviter toute collision. À relire au réveil pour voir où en est ce chantier séparé.
